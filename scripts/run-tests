#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")/.."
cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug
cmake --build build --parallel --target dearapi_tests
ctest --test-dir build --output-on-failure --verbose
